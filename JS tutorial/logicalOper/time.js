let hour = +prompt('Enter the hour', 00);
let minutes = +prompt('Enter the minut', 00);

if (+hour == 12 && +minutes == 30) {
    alert(`Time is ${hour}:${minutes}`);
}
else {
    alert(`Time is ${hour}:${minutes}`);
}